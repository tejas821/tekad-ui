// LEGAL: this project is tagged type:charts, so the charting import is allowed.
// (The import is type-only so the fixture needs no chart.js install.)
import type { Chart } from 'chart.js';
export type ChartsFixture = Chart;
