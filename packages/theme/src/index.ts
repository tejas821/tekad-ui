/**
 * `@tekad/theme` — primary entry point.
 *
 * The shipped artefact of this package is `styles/tekad.css`. The TypeScript
 * surface is deliberately two symbols: the scheme attribute name and its type.
 * Everything else the theme does, it does in CSS.
 */
export { TEKAD_SCHEME_ATTRIBUTE, type TekadScheme } from './lib/scheme';
