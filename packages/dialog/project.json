{
  "name": "dialog",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/dialog/src",
  "prefix": "tk",
  "projectType": "library",
  "tags": ["layer:component"],
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/packages/dialog"],
      "options": {
        "project": "packages/dialog/ng-package.json",
        "tsConfig": "packages/dialog/tsconfig.lib.json"
      },
      "configurations": {
        "production": {},
        "development": {}
      },
      "defaultConfiguration": "production"
    },
    "test": {
      "executor": "@nx/angular:unit-test",
      "options": {
        "buildTarget": "dialog:build",
        "tsConfig": "packages/dialog/tsconfig.spec.json",
        "runner": "vitest",
        "include": ["**/*.spec.ts"]
      }
    }
  }
}
