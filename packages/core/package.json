{
  "name": "@tekad/core",
  "version": "0.0.0",
  "description": "TEKAD foundation. Nothing here may depend on a component package.",
  "//peerDependencies": [
    "@angular/core is deliberately NOT declared. Nothing in this package imports",
    "it yet: the identity primitive is plain TypeScript. Declaring an unused peer",
    "makes every consumer install something the package never touches, and the",
    "@nx/dependency-checks lint rule flags it — correctly. It gets added the",
    "moment a symbol here actually needs Angular."
  ],
  "license": "Apache-2.0",
  "sideEffects": false,
  "private": true,
  "dependencies": {
    "tslib": "^2.8.1"
  }
}
