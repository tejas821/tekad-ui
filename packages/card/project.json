{
  "name": "card",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/card/src",
  "prefix": "tk",
  "projectType": "library",
  "tags": ["layer:component"],
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/packages/card"],
      "options": {
        "project": "packages/card/ng-package.json",
        "tsConfig": "packages/card/tsconfig.lib.json"
      },
      "configurations": {
        "production": {},
        "development": {}
      },
      "defaultConfiguration": "production"
    },
    "test": {
      "executor": "@nx/angular:unit-test",
      "options": {
        "buildTarget": "card:build",
        "tsConfig": "packages/card/tsconfig.spec.json",
        "runner": "vitest",
        "include": ["**/*.spec.ts"]
      }
    }
  }
}
